interface ILink {
  linkUrl: string;
  linkTxt: string;
}

interface ITitle {
  title: string;
}

interface ICardCategorie {
  name: string;
  image: string;
}

export type { ICardCategorie,ILink, ITitle };
