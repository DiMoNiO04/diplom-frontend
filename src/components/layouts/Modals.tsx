import { ModalAuth, ModalChangePassword, ModalConfirm } from '../modals';

export const Modals = () => {
  return (
    <>
      <ModalAuth />
      <ModalConfirm />
      <ModalChangePassword />
    </>
  );
};
